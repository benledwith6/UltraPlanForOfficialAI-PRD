import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function CalendarPage() {
  return (
    <PlaceholderPage
      eyebrow="Calendar"
      title="Guided cadence calendar is staged for build item 5."
      description="This route exists now so navigation is stable while onboarding is reviewed."
    />
  );
}
