import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function BrainstormPage() {
  return (
    <PlaceholderPage
      eyebrow="Brainstorm"
      title="Card-based brainstorm arrives in build item 7."
      description="The PRD recommendation is card-based proposals, exposed behind config so the UX can flip without redeploy."
    />
  );
}
