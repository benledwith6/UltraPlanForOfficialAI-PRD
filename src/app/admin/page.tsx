import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function AdminPage() {
  return (
    <PlaceholderPage
      eyebrow="Admin"
      title="Internal admin dashboard is staged for build item 8."
      description="This route will become the model evaluation grid, job log, failure dashboard, template manager, support view, and feature flag surface."
    />
  );
}
