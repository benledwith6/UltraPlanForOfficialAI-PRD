import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function AdminModelEvalsPage() {
  return <PlaceholderPage eyebrow="Admin" title="Model x template eval grid" description="Staged for build item 8." />;
}
